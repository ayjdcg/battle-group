# 兵团战役数值模型

## 目的与边界

本文件把未来完整的战争经营数值体系，与下一阶段可直接实现的最小补给模型分开。目标不是立刻模拟现实战争，而是让玩家能理解并利用一个稳定的战略循环：

> 兵团行军和作战会消耗补给 → 补给线与补给节点决定恢复 → 断供兵团逐步失去机动与战斗能力 → 占领、驻防、迂回和切断后路都具有代价与价值。

除特别标注外，所有百分比型状态取值为 `0–100`，资源量不得为负数。属性应只有一个权威来源：单位定义提供基础能力，兵团保存当前状态，阵营保存全局库存，节点保存地理与后勤能力。UI 只展示和调用这些数据，不能另存一套结算值。

## 对象与职责

| 对象 | 回答的问题 | 保存的主要数据 |
|---|---|---|
| 单位 | 这个编制槽提供什么能力、需要什么资源？ | 类型、生命、攻击、速度、补给需求、特性 |
| 兵团 | 这支部队现在能做什么、状态如何？ | 九槽编制、兵力、补给、士气、位置、命令、指挥官 |
| 节点 | 这里能通行、战斗、驻防和获得什么？ | 容量、地形、控制方、补给能力、库存、产出 |
| 阵营/玩家 | 全局还能提供什么、从哪里投送？ | 司令部、人力、补给总库、装备库（未来）、控制节点 |
| 指挥官 | 这支兵团采用什么独特战术/后勤倾向？ | 特性、等级（未来）、伤亡状态（未来） |

## 完整数值模型（目标架构）

### 1. 单位

每个单位槽可包含一个单位实体或一个小队实体。单位的战损最终汇总为兵团战力和补充需求。

| 类别 | 属性 | 说明 |
|---|---|---|
| 身份 | `id`、`type`、`label`、`tags` | `tags` 例如步兵、装甲、空中、医疗 |
| 生存 | `hp`、`maxHp`、`destroyed` | 当前生命、最大生命、是否彻底损失 |
| 战斗 | `damage`、`cooldown`、`armor`、`range`、`targetRules` | 伤害、攻击周期、防护、射程和选敌规则 |
| 支援 | `heal`、`repair`、`antiArmor`、`antiAir` | 医疗、维修、反装甲、防空等专门能力 |
| 机动 | `speed`、`terrainModifiers` | 基础速度及地形修正 |
| 消耗 | `supplyUseMove`、`supplyUseCombat`、`supplyUseGarrison` | 行军、战斗、驻防的补给需求 |
| 补充 | `personnelCost`、`equipmentCost`、`repairCost` | 恢复该单位所需的人力、装备和维修资源 |

`hp` 表示该槽内单位的战斗完整度；人力不必在单位层重复保存。需要补充的人力可由损失生命、单位类型和编制规则换算。

### 2. 兵团

| 类别 | 属性 | 说明 |
|---|---|---|
| 编制 | `slots[9]`、`units`、`doctrine` | 九槽编制、实际单位、可选战术学说 |
| 人员状态 | `personnel`、`maxPersonnel`、`casualties` | 可由单位状态推导；需要显示时缓存 |
| 后勤 | `supply`、`maxSupply`、`supplyNeed`、`supplyReceived` | 携带量、容量、本周期需求、实际获得量 |
| 组织状态 | `morale`、`fatigue`、`organization` | 士气、疲劳、组织度；后两项可后置 |
| 战力汇总 | `combatPower`、`defense`、`speed` | 每次结算从单位、状态、地形和指挥官计算 |
| 战役状态 | `nodeId`、`path`、`destinationId`、`status` | 所在节点、路线、目标和当前命令 |
| 控制/战斗 | `garrisoning`、`engagementId`、`pendingEncounter` | 驻防、参与战斗、待处理遭遇 |
| 指挥 | `commanderId`、`commanderTraits` | 只提供可解释的规则修正，非独立地图单位 |

兵团的关键派生量：

```text
兵团速度 = 最慢存活单位速度 × 规模系数 × 士气/补给就绪系数 × 地形系数 × 指挥官修正
战斗力 = 存活单位战力之和 × 士气系数 × 补给系数 × 地形/驻防/指挥官修正
```

### 3. 节点与通路

| 类别 | 属性 | 说明 |
|---|---|---|
| 身份与归属 | `id`、`type`、`team`、`controller` | 节点类型、初始归属与当前控制方 |
| 战斗 | `capacity`、`defenseModifier`、`cover` | 同时有效参战兵团数、防御和掩护 |
| 后勤 | `supplySource`、`supplyStorage`、`maxSupplyStorage`、`supplyOutput` | 是否为补给源、当前/最大库存、周期产出 |
| 经营（未来） | `manpowerOutput`、`equipmentOutput`、`repairCapacity` | 城镇、工业区等长期产出 |
| 网络 | `edges`、`moveCost`、`supplyThroughput` | 通路、行军成本、单位时间输送上限 |

节点控制权不等于“有兵团路过”；应由驻防或规则规定的明确占领行为改变。敌方控制、被破坏通路、封锁战斗和吞吐上限都会影响补给线。

### 4. 阵营/玩家

| 类别 | 属性 | 说明 |
|---|---|---|
| 指挥中枢 | `hqNodeId`、`commandCapacity` | 司令部位置、未来指挥容量 |
| 全局资源 | `manpower`、`supplyStock`、`equipmentStock` | 人力、通用补给、装备库存 |
| 周期产出 | `manpowerIncome`、`supplyIncome`、`equipmentIncome` | 由控制节点和主城提供 |
| 后勤网络 | `supplyNodes`、`supplyRoutes` | 已激活补给节点与计算后的有效线路 |
| 情报（未来） | `visibility`、`intel` | 视野与敌情精度 |

### 5. 指挥官

指挥官绑定兵团，不占九槽，不单独行动。特性应是清晰、少量、可显示的规则修正，例如：

| 特性方向 | 示例效果 |
|---|---|
| 进攻 | 首轮攻击提高；强行通过额外损失降低 |
| 防御 | 驻防防御提高；撤退损失降低 |
| 后勤 | `maxSupply` 提高；补给恢复提高；断供惩罚延后 |
| 机动 | 行军速度提高；绕路代价降低 |
| 士气 | 战败士气损失降低；预备队投入更快 |

升级、装备、稀有度、随机抽取、俘虏/阵亡等系统都不属于 P2。

## 最小实现模型（P2）

P2 只实现“人力 + 通用补给”两种可库存资源。兵团不直接消耗独立的弹药、口粮、燃料或维修件；它们先统一抽象为 `supply`。

### 现在保留的字段

```js
unit: {
  id, type, hp, maxHp, destroyed,
  speed, damage, cooldown
}

corps: {
  id, team, slots, units,
  supply: 100, maxSupply: 100,
  morale: 100,
  nodeId, path, destinationId, status,
  garrisoning, engagementId, pendingEncounter,
  commanderId: null, commanderTraits: [] // 仅预留，不在 P2 生效
}

faction: {
  hqNodeId: 'blue-hq',
  manpower: 1000,       // P2 可只显示，暂不提供补兵 UI
  supplyStock: 500      // P2 的真实全局补给来源
}

node: {
  id, type, controller,
  supplySource: false,  // 司令部为 true；少数补给节点为 true
  supplyOutput: 0       // 每个结算周期可向本节点/邻接兵团提供的补给
}
```

### P2 规则

以固定的战役结算周期执行（例如每 1 秒游戏时间，或每抵达一个节点）。所有数值先集中在常量表中，方便调试，不散落在 UI 代码。

| 情况 | 补给变化 | 状态影响 |
|---|---:|---|
| 待命 | -0.1 / 周期 | 无或极低影响 |
| 行军 | -0.5 / 周期 | 补给低时减速 |
| 战斗 | -1.2 / 周期 | 补给低时战斗力降低 |
| 驻防 | -0.25 / 周期 | 断供时无法长期维持驻防 |
| 与己方补给源连通 | 恢复至多 +1.5 / 周期 | 从阵营 `supplyStock` 扣除相同数量 |
| 位于己方补给节点 | 额外恢复至多 +2.5 / 周期 | 同样受节点产出与阵营库存限制 |

建议的补给阈值：

| 当前补给 | 状态 | 效果 |
|---:|---|---|
| 51–100 | 充足 | 无惩罚 |
| 26–50 | 紧张 | 速度 × 0.9，战斗力 × 0.9 |
| 1–25 | 匮乏 | 速度 × 0.7，战斗力 × 0.7，士气额外下降 |
| 0 | 断供 | 不可主动驻防；速度 × 0.5，战斗力 × 0.5，持续损失士气 |

补给线的最小定义：从兵团所在节点出发，能否沿通路到达同阵营控制的司令部或 `supplySource: true` 节点。路径不得经过敌方控制节点；正在交战的节点可先视为不可通行。第一版忽略线路距离、通路吞吐、节点库存和敌军拦截概率。

### P2 暂不实现

- 独立的弹药、燃料、食物、药品、维修件；
- 人力补充的完整 UI 与编制重建；
- 装备库存、坦克维修、武器生产；
- 疲劳、组织度、疾病、天气；
- 通路运输量、补给车队、线路破坏；
- 指挥官技能实际效果与升级；
- 复杂的敌方后勤 AI。

## 从最小模型扩展到完整模型

扩展必须在已有规则可玩且数值可观察后进行。推荐顺序：

1. **P2 最小补给闭环**：补给线、恢复、消耗、阈值惩罚、补给节点价值。
2. **补兵与修复**：消耗阵营人力和未来装备库存，恢复受损单位；补给不能直接复活兵员。
3. **节点库存与运输量**：补给节点有库存，长补给线和多兵团竞争会产生吞吐压力。
4. **拆分资源**：当装甲、空中单位和主城生产真的带来不同决策时，再拆为燃料、弹药与维修件；步兵仍可主要消耗通用补给。
5. **指挥官与战术深化**：只在核心循环稳定后，让指挥官改变明确的一两条规则。

## 验收与调试指标

P2 完成时，玩家应能在界面上看清每支兵团的当前补给、最大补给、是否连通补给源、当前惩罚与下一次恢复来源。至少验证：

- 连通司令部的兵团可恢复补给，但会消耗阵营库存；
- 占领补给节点能让前线恢复更快或延伸补给线；
- 切断路径后，兵团会因行军、战斗和驻防逐步进入补给阈值；
- 断供不会立即消灭兵团，但会显著降低推进与作战能力；
- 强行通过战区不会成为无成本捷径：其损耗会与后续补给恢复压力形成连续代价。

## 设计决策记录

- **P2 使用一个通用补给值。** 这是有意的抽象，不是遗漏。只有当拆分资源能带来新的、可理解的决策时才增加复杂度。
- **人力必须独立于补给。** 补给维持行动，兵员/装备用于恢复战损与扩编。
- **士气是状态而不是库存。** 它可以受补给、胜败和指挥官影响，但不能被运输或直接从仓库补充。
- **派生数值尽量实时计算。** 战斗力、速度、补给需求等避免长期缓存，防止单位损失、状态与显示不一致。
