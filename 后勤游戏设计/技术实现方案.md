# 后勤游戏 · 技术实现方案

> 配套文件：`机制库.md`（机制为什么存在）、`Demo 切割线.md`（这一版做什么、数值起点）、`运输与人员循环机制设想.md`（往返结构，不在本文件重复展开）。
> 本文件只回答一件事：**照什么顺序写代码，每一步写成什么样。**
> 设计判断已经在前两份文件和往返结构文件里做完了。执行这份文件时不需要再做设计判断——只需要照抄数据结构、照抄公式、照抄顺序。凡是本文件里出现"暂定/建议"字样的地方，才是本文件自己做的补充决定，其余全部来自前两份文件。
~~# 目前已经完成M8，M9未开始。这句主要是提醒我和agent当前的进度~~

**修正（2026-09-17）**：M8 已完成；针对首轮试玩的 M8.5“压力剧本 + 持久战线 + 公平归因 + 终局评价”已实现，待人工试玩调参；M9 十局自测尚未开始。原因：M8 的“能跑30天”把时间走完错当成结果，需先补齐胜负闭环再做 M9。影响范围：进度标记、M6 战报验收、M8.5 验收与 M9 试玩入口。

~~**修正（2026-09-18）**：往返循环尚未落地到代码。当前实现仍是人员不占运力、伤员瞬移进医院、运力按天刷新。下一轮实现必须按本节删除线改回，不得沿用这三项。影响范围：M5/M8 验收、状态结构、发运与医院结算。~~

**修正（2026-09-18）**：往返循环已落入 `logistics-core.js`：`transitQueue` 区分去程/回程并占用 `fleetSize`；伤员进各师 `evacQueue`；医院只接收回程卸载；补员占去程并进入整编队列；`availableTransport()` = 编制减去在途。UI 操作对象改为可调度车队。影响范围：发运、队列、战斗、地图 UI、M5/M8 测试。
---

## 零、执行纪律（先读这一节，再动手）

1. **不改旧代码。** `campaign-core.js` / `campaign-ui.js` / `map-generator.js` / `campaign-core.test.js` / `index.html` / `defense-demo.html` / `map-generator-demo.html` 一个字都不要动。这是一个全新的、独立的原型，只新增文件（原因见机制库「系统 4·黑箱定位」与「十、可复用的旧资产」：旧代码是自动战斗引擎，这个游戏里战斗是黑箱，不需要它）。
2. **数字只有一个来源。** 所有数值起点抄自 `Demo 切割线.md` 第四节，写进本文件第五节的常量表后就不要在别的地方重复写死同一个数字。以后调参只改常量表这一处。
3. **严格按第九节的里程碑顺序做，不要跳步，也不要提前实现后面里程碑要用到的机制。** 每个里程碑结束必须能跑起来（页面打得开、测试跑得过）才能进入下一个。
4. **本文件的范围 = `Demo 切割线.md` 第二节"进"的那一列。** 没在那张表里的机制，出现在 `机制库.md` 里也不要实现，直接跳过，不用问。
5. 命名、代码风格跟着仓库现有 JS 文件走（见第二节），不要引入新框架、新构建工具、新依赖。

---

## 一、这是不是要重新设计一遍？不是

这份文件不重复 `机制库.md` 和 `Demo 切割线.md` 里已经讲清楚的"为什么"。下面每一节只做一件事：把设计文档里的一句话或一张表，翻译成"哪个文件、哪个函数、哪个字段"。如果你在实现时发现某个机制在设计文档里没写清楚具体数字或规则，去 `机制库.md` / `Demo 切割线.md` 里找，不要自己编——找不到就在第十一节「实现时的模糊点与暂定选择」里已经列出了本文件替你做的补充假设；不在那张清单里的模糊点，先停下来问，不要猜。

---

## 二、新增文件清单

全部新建于仓库根目录，与现有 `campaign-core.js` 等文件同级（不新建子目录，不放进 `后勤游戏设计/`，那个目录只放设计文档）。命名风格跟随现有文件（英文短横线小写），逻辑与测试用 CommonJS + 浏览器 UMD 双兼容写法，与 `map-generator.js` 顶部的写法完全一致地抄一遍：

| 文件 | 作用 | 对应旧项目里的谁 |
| --- | --- | --- |
| `logistics-constants.js` | 全部数值起点、枚举表。只导出常量，不含逻辑 | 无对应，旧项目把常量散落在 `campaign-core.js` 顶部，这次单独拆出来是因为「数值系统是长出来的，要经常改」（机制库 九） |
| `logistics-core.js` | 纯规则：状态结构、每日主循环九步、所有计算函数 | `campaign-core.js` |
| `logistics-core.test.js` | `node:assert` 测试，跑法一致：`node logistics-core.test.js` | `campaign-core.test.js` |
| `logistics-ui.js` | 读状态画监护仪面板、收集玩家当天的五项指令、调用 core 推进一天 | `campaign-ui.js` |
| `logistics-demo.html` | 页面入口 | `defense-demo.html` |

`package.json` 与 `vite.config.js` 需要各加一行：

```json
"scripts": { "test:logistics": "node logistics-core.test.js" }
```

```js
rollupOptions: { input: { campaign: 'index.html', defense: 'defense-demo.html', logistics: 'logistics-demo.html' } }
```

不要把 `test:logistics` 合并进现有 `test` 脚本——两套规则要能独立跑、独立失败，合并了出错不好定位是谁的问题。

---

## 三、地图与运输线（固定数据，不写生成器）

`Demo 切割线.md` 三、Demo 规格 / 地图：后方基地 ×1、中转节点 ×2、前线节点 ×3、铁路 ×1、公路 ×2。**这是一张写死的常量，不需要 `map-generator.js` 那种随机生成器**，因为该生成器的校验规则（20–32 个节点、三线战斗地形、专供自动战斗用）跟这张 6 节点小图完全不匹配，硬套反而增加复杂度。

`机制库.md`「系统 1」讲的"边的四属性"（吞吐、在途天数、脆弱度、载荷）是**运输线级别**的属性，不是逐段路口的属性——demo 里中转节点不单独占用吞吐或装卸时间（拥堵非线性、中转装卸明确不进 demo，见 `Demo 切割线.md` 表格），中转节点只在战报文案里出现（"车队经过中转站 A"），不参与任何数值计算。据此把"1 铁路 + 2 公路 + 2 中转 + 3 前线"钉成三条运输线：

```js
// logistics-constants.js
export const MAP = {
  base: { id: 'base', name: '后方基地' },
  transitNodes: [
    { id: 'transit-a', name: '中转站甲' },
    { id: 'transit-b', name: '中转站乙' },
  ],
  divisions: [
    { id: 'div-1', name: '第一师', frontNodeId: 'front-1' },
    { id: 'div-2', name: '第二师', frontNodeId: 'front-2' },
    { id: 'div-3', name: '第三师', frontNodeId: 'front-3' },
  ],
  // 每条线路 = 基地到一个师防区的完整路由。throughput 单位见第五节「运力单位 D」。
  lines: [
    { id: 'line-rail',  type: 'rail', divisionId: 'div-1', via: 'transit-a', throughputPerDay: 100, transitDays: 1, fragility: 'high' },
    { id: 'line-road-1',type: 'road', divisionId: 'div-2', via: 'transit-a', throughputPerDay: 40,  transitDays: 2, fragility: 'low'  },
    { id: 'line-road-2',type: 'road', divisionId: 'div-3', via: 'transit-b', throughputPerDay: 40,  transitDays: 2, fragility: 'low'  },
  ],
};
```

~~每个师固定绑定一条运输线（demo 不做多路径选择——`Demo 切割线.md` 没有把"多路径分流"列入"进"的机制，「命令」第 1 条"调整各条线路的物资配比"指的是"这条线上弹药/给养/人员各占多少"，不是"选哪条线"）。三条线的吞吐单位在第五节换算成统一的 `D`。~~

**修正（2026-09-17）**：每个师仍固定绑定一条运输线，但线路配比只包含弹药与给养；人员走“国家动员 → 训练 → 人员池 → 建制消化”的独立管线，不占物资车队运力。原因：当前规则的自动发运实际只装载弹药/给养，原文把人员写进线路配比会让 UI 暗示一个并不存在的人员运输选择。影响范围：`orders.lineRatios`、线路配置 UI、在途总览、M8 UI 验收测试；人员分配仍由 `personnelAssignment` 完成。

**修正（2026-09-18）**：撤回“人员不占物资车队运力”。线路去程构成改回弹药 / 给养 / 补员，合计 1；`personnelAssignment` 只决定去程人员装给谁，不直接改满编率。结构见 `运输与人员循环机制设想.md`。原因：独立管线把人员做成第二套账。影响范围：`orders.lineRatios`、`transitQueue.cargo`、发运预览、M8 人员线路隔离断言（该断言作废）。

---

## 四、核心状态结构（`GameState`）

这是整局唯一的数据源，UI 只读它、玩家指令只改它的 `orders` 字段、`advanceDay()` 是唯一允许推进它的函数。字段名以后写代码时不要改，改了要同步改这份文件。

~~原状态结构把 `transitQueue.cargo` 写成 `{ammo,supply,personnel}`，并把 `lineRatios` 写成弹药 0.6 / 给养 0.3 / 人员 0.1。~~ **修正（2026-09-17）**：物资在途只含弹药与给养，线路默认比例改为 2/3 与 1/3；人员状态只进入训练、医院、人员池和建制消化队列。原因与影响范围同第三节修正。

**修正（2026-09-18）**：恢复 `transitQueue.cargo.personnel`。批次必须区分去程/回程；回程载荷为伤员或轮换人员。每师增加 `evacQueue`（待后送）。`hospitalQueue` 只接收回程卸载。`orders.lineRatios` 恢复三人分项；新增 `orders.returnPriority`（默认伤员优先）。在途车辆从可调度运力中扣除，回程抵达基地后才加回。原因：2026-09-17 的拆分把循环做成两本账。影响范围：`GameState`、`dispatchShipments`、`advanceQueues`、`resolveBattles`、M5/M8 测试。

```js
const state = {
  day: 0,                    // 从 0 开始，每次 advanceDay() 后 +1，跑到 30 结束
  ended: false,

  base: {
    ammo: 0, supply: 0,      // 基地库存，单位 D（见第五节），对玩家精确可见、无延迟
  },

  quota: {
    periodDays: 7,           // 配额周期
    nextArrivalDay: 7,       // 下一次到货日
    nextAmount: 0,           // 下一批数量（提前 3 天变为精确值，之前是 null）
    pendingAdvanceDebt: 0,   // 已预支、将从未来配额里扣除的量
  },

  personnel: {
    pool: 0,                 // 人员池：新兵与康复老兵共用，等待分配的人数（demo 用抽象人数单位，见第五节）
    recoveredVeterans: 0,    // 已由医院救回、无需训练的人员点数（同时计入 pool）
    permanentLosses: 0,      // 医院及后续结算产生的不可回收人员点数
    mobilization: { intervalDays: 21, nextArrivalDay: 21, nextAmount: 30 },
    trainingQueue: [],       // [{ id, count, track: 'urgent'|'normal', arrivesDay }]
    hospitalQueue: [],       // [{ id, count, arrivesDay, overflowDays: 0 }] 只接收回程卸载，不得由战斗直接写入
    integrationQueue: [],    // [{ id, divisionId, count, arrivesDay, effPenalty }]
    // 2026-09-18：待后送不在 personnel 根上，挂在各师 evacQueue
  },

  transitQueue: [],          // 往返批次：[{ id, lineId, divisionId, leg: 'outbound'|'return', cargo: {ammo,supply,personnel}, dispatchedDay, arrivesDay }]

  divisions: [
    {
      id: 'div-1', name: '第一师',
      personality: { aggression: 0, hoarding: 0, obedience: 0, selfAwareness: 0 }, // 0~1，见第五节三种性格预设
      posture: 'defend',           // 'attack'|'active-defend'|'defend'|'dormant'|'refit'|'retreat'
      manpowerRatio: 1,            // 兵员满编率 0~1
      ~~ammoDays: 0, supplyDays: 0,~~  // 弹药/给养储备，单位：天
      ammoDays: 8, supplyDays: 6,
      fatigue: 0,                  // 疲劳度 0~1
      equipmentReady: 1,           // 装备完好率 0~1
      integrationEffPenalty: 1,    // 建制消化中的额外效能系数（0.8 或 1）
      underAttack: false,
      quietDays: 0,                // 连续未接敌天数，供自主进攻判定使用
      frontMovement: 0,            // 当日战斗的战线变化，正数前进、负数后退（公里）
      frontPosition: 0,            // 持久累计的战线位置，不被下一场战斗覆盖
      casualtiesTotal: 0, battles: [], collapsed: false, lowAmmoDays: 0,
      lastReportDay: 0,            // 玩家看到的这份战报对应哪一天（延迟来源）
      lastReportSnapshot: null,    // 玩家可见的延迟状态快照；UI 读取本字段而非实时师状态
      reportHistory: [],           // 按天保存的师状态快照，供信息延迟回溯（UI 不直接读取）
      perceivedSelf: null,         // 该师"自认为"的状态快照（自知力误差，AI 内部用）
      evacQueue: [],               // 2026-09-18 待后送：[{ id, count, kind: 'wounded'|'rotation', sinceDay }]
    },
    // div-2, div-3 同构
  ],

  orders: {                  // 玩家当天下的指令，advanceDay() 读取后清空/保留到下一次修改
    lineRatios: {             // 每条线：弹药/给养/补员占比，合计 1
      'line-rail': { ammo: 0.6, supply: 0.3, personnel: 0.1 },
      'line-road-1': { ammo: 0.6, supply: 0.3, personnel: 0.1 },
      'line-road-2': { ammo: 0.6, supply: 0.3, personnel: 0.1 },
    },
    divisionPriority: { 'div-1': 1, 'div-2': 1, 'div-3': 1 }, // 相对优先级权重，运力不够时按此分配
    minReserveDays: { 'div-1': 3, 'div-2': 3, 'div-3': 3 },   // 最低储备线（天）
    trainingTrack: 'normal',        // 'urgent'|'normal'，本轮新兵入训练营走哪档
    personnelAssignment: {},        // { 'div-1': count, ... } 去程人员装给谁，不直接改满编率
    returnPriority: { wounded: 1, rotation: 0 }, // 2026-09-18 回程装载规则，默认伤员优先
    briefTruth: {},                 // { 'div-1': true/false } 是否回电告知真相
    dailyShipmentCap: Infinity,     // 今日发运总量上限，可低于当前可调度运力甚至为 0
    advanceQuota: 0,                // 本次预支的数量（0 表示不预支）
  },

  battlePreview: [],          // 战斗预告：[{ divisionId, day, source: 'orders-known'|'enemy-signal', certainty }]
  eventLog: [],                // 每日战报，UI 从这里读历史
  ~~counterfactualCache: {},~~ // 修正（2026-09-17）：当前版不再生成无条件事后反事实，改存 `frontRequests`的公开日、战斗日、申请量与到货量。原因：只对当时已知条件做可审计归因。影响范围：GameState、战报与 M6/M8.5 测试。
};
```

**关于"抽象人数单位"**：`机制库.md` 没给人员池的具体单位（是"人"还是"编制百分比点"）。`Demo 切割线.md` 数值起点里医院日处理名额写的是"1 D 对应人数的 20%"，说明人员也换算到与 D 相关的一个抽象单位上。本文件的暂定选择：**把一个满编师定义为 100 点满编，`manpowerRatio` 就是这 100 点里还剩几成**；人员池、训练队列、医院队列全部用"点数"计数，1 点 = 1% 满编。这样"1 D 对应人数的 20%"就能直接读成"20 点"，不用引入第二套换算表。这是本文件做的补充假设，不是设计文档原文，如果后续设计文档给出了明确单位，以设计文档为准并回来改这一段。

---

## 五、常量表（唯一数值来源，抄自 `Demo 切割线.md` 第四节）

```js
// logistics-constants.js

// ---- 运力单位 D：三个师全部处于"积极防御"时的一日总消耗 ----
export const TOTAL_TRANSPORT_PER_DAY = 1.0;      // D/天，恒定
export const INITIAL_BASE_INVENTORY = 8.0;       // D，初始库存（按 65/35 拆成弹药/给养，见下）
export const QUOTA_PERIOD_DAYS = 7;
export const QUOTA_PER_PERIOD = 6.0;             // D/期
export const QUOTA_COMPOSITION = { ammo: 0.65, supply: 0.35 }; // 固定错配构成，demo 不给玩家改
export const QUOTA_ADVANCE_MAX_RATIO = 0.5;      // 最多预支下一期的 50%
export const QUOTA_ADVANCE_INTEREST = 1.5;       // 预支 1 D，下一期扣 1.5 D
export const QUOTA_PREVIEW_LEAD_DAYS = 3;        // 下一期给准确数量的提前天数

// ---- 人员环路 ----
export const MOBILIZATION_INTERVAL_DAYS = 21;
export const MOBILIZATION_BATCH_SIZE = 30; // 暂定：每批 30 个满编点，见十一
export const TRAINING_DAYS = { urgent: 0, normal: 7 };
export const TRAINING_COMBAT_MULT = { urgent: 0.5, normal: 1.0 };
export const TRAINING_CASUALTY_MULT = { urgent: 1.3, normal: 1.0 };
export const CASUALTY_SPLIT = { kia: 0.3, wounded: 0.7 }; // 阵亡/失踪 vs 负伤
export const MEDIC_RECOVERY_RATE = 0.7;                    // 军医转化率
export const HOSPITAL_DAILY_QUOTA_RATIO = 0.2;              // 1 D 对应人数的 20%
export const HOSPITAL_OVERFLOW_DEATH_MULT = 1.2;            // 顺延期间死亡率 ×1.2（对应 +20%）
export const INTEGRATION_LADDER = [                          // 建制消化阶梯，按 manpowerRatio 分档
  { min: 0.5, max: 1.0,  delayDays: 0, effPenalty: 1.0 },
  { min: 0.2, max: 0.5,  delayDays: 2.5, effPenalty: 1.0 },  // 2–3 天取中值，试跑时可在 2/3 之间调
  { min: 0,   max: 0.2,  delayDays: 5,  effPenalty: 0.8 },
];
export const LOW_AMMO_CASUALTY_MULT = 1.2; // 弹药 <5% 档位时伤亡率 ×1.2，人员战斗减员不随弹药下降而下降

// ---- 消耗倍率（按姿态） ----
// 数值起点原文把"底噪"与"固守"列成同一行给了两个数字 1.0 / 2.0，未逐字说明分别对应哪种物资。
// 本文件的暂定拆分（见十一「模糊点」）：固守姿态下，给养倍率 1.0（底噪，几乎不因固守额外增加），
// 弹药倍率 2.0（固守仍有警戒/巡逻消耗）。以后试玩如证明拆分不对，只改这一处。
export const POSTURE_CONSUMPTION_MULT = {
  refit:          { ammo: 0.8, supply: 0.8 },
  defend:         { ammo: 2.0, supply: 1.0 },   // "固守"＝设计文档里的底噪行
  dormant:        { ammo: 1.2, supply: 1.2 },
  'active-defend':{ ammo: 3.0, supply: 3.0 },
  attack:         { ammo: 7.0, supply: 7.0 },   // 6–8× 取中值 7，调参时可在 6~8 间试
  retreat:        { ammo: 1.0, supply: 1.0 },   // "移动消耗"，暂定与固守同量级，纯占位
};
export const SPIKE_EXTRA_DAYS_EQUIVALENT = 3; // 单个高强度交战日额外吃掉约 3 天的量

// ---- 师 AI 的暂定决策阈值 ----
export const REFIT_FATIGUE_THRESHOLD = 0.75;
export const OBEDIENCE_FORCED_ATTACK_THRESHOLD = 0.7;
export const AUTONOMOUS_ATTACK_AGGRESSION_THRESHOLD = 0.7;
export const BATTLE_PREVIEW_LEAD_DAYS = 3;
export const ENEMY_EFFECTIVENESS = 0.7; // 暂定固定敌军基准，见十一
export const BATTLE_OUTCOME_TABLE = [
  { minRatio: 1.5, frontMovement: 8,  casualtyRate: 0.02 },
  { minRatio: 1.0, frontMovement: 3,  casualtyRate: 0.04 },
  { minRatio: 0.7, frontMovement: -3, casualtyRate: 0.08 },
  { minRatio: 0,   frontMovement: -8, casualtyRate: 0.15 },
];

// 疲劳曲线：设计原文未给具体斜率，M3 先采用确定性线性起点，试玩后只在此处调参。
export const POSTURE_FATIGUE_CHANGE = {
  refit: -0.12, defend: 0.01, dormant: -0.03,
  'active-defend': 0.035, attack: 0.07, retreat: 0.04,
};

// ---- 弹药档位（阶梯） ----
export const AMMO_TIER = [
  { min: 0.5, max: Infinity, combatMult: 1.0,  canAttack: true  },
  { min: 0.2, max: 0.5,      combatMult: 0.7,  canAttack: false },
  { min: 0.05,max: 0.2,      combatMult: 0.35, canAttack: false },
  { min: 0,   max: 0.05,     combatMult: 0.1,  canAttack: false, collapseAfterDays: 3 },
];
// 注意：AMMO_TIER 的 min/max 单位是"占满编基准储备的比例"还是"天数"，数值起点写的是百分号（≥50%、
// 50–20% ...），但状态字段 ammoDays 是"天数"。暂定换算：把"该师安全储备基准"定义为
// DIVISION_SAFE_RESERVE_DAYS（见下），ammoDays / DIVISION_SAFE_RESERVE_DAYS 得到比例后再查表。
export const DIVISION_SAFE_RESERVE_DAYS = 10; // 暂定值，试跑时随初始库存/配额刻度一起调

// ---- 效能公式 ----
// 效能 = 满编率 × 弹药档位 × 装备完好率 × (1 - 疲劳)   （demo 不含军官系数）

// ---- 其他 ----
export const INFO_DELAY_DAYS = { normal: 1, inCombat: 2 };
export const BATTLE_PREVIEW_LEAD_DAYS = 3;
export const GAME_LENGTH_DAYS = 30;

// ---- 三个师的性格预设（机制库「性格四参数」 + Demo 切割线"三个师性格刻意拉开"） ----
export const DIVISION_PERSONALITY_PRESETS = {
  aggressive:  { aggression: 0.8, hoarding: 0.2, obedience: 0.5, selfAwareness: 0.6 }, // 冲锋型
  hoarder:     { aggression: 0.2, hoarding: 0.85, obedience: 0.5, selfAwareness: 0.6 }, // 铁公鸡
  obedient:    { aggression: 0.4, hoarding: 0.4, obedience: 0.9, selfAwareness: 0.2 }, // 老实人
};
```

`DIVISION_SAFE_RESERVE_DAYS` 与 `AMMO_TIER` 的单位换算是本文件替设计文档做的补充决定（原文档只给了百分号阶梯，没给"天数 → 百分号"的换算基准），第十一节会再提一遍，方便以后审查。

---

## 六、每日主循环九步：函数签名 + 伪代码

对应 `Demo 切割线.md` 五、每日主循环（实现顺序），一步一个函数，`advanceDay(state)` 按顺序调用它们，不要合并、不要打乱顺序（顺序本身是设计——例如"配额结算"必须在"师的请求"之前，否则师看到的库存是昨天的）：

```js
function advanceDay(state) {
  state.day += 1;                          // 1. 推进日期
  settleQuota(state);                      // 2. 配额结算
  settleMobilization(state);                // 3. 国家动员结算
  advanceQueues(state);                     // 4. 去程卸载并自动装回程；回程卸载入医院/休整，车辆回可调度
  for (const division of state.divisions) {
    perceive(state, division);              //   5a. 感知（带自知力偏差）
    decidePosture(state, division);         //   5b. 决策姿态（性格加权规则表）
    const request = generateRequest(state, division); //   5c. 生成请求（含囤积/抢运力放大）
    consumeAndApplyPosture(state, division); //   5d. 按姿态消耗
    autoAllocateArrivedCargo(state, division); //   5d'. 前线自行分配（见系统 5）
    reportUp(state, division);              //   5e. 上报（精度由延迟决定）
  }
  resolveBattles(state);                    // 6. 交战结算；负伤入 evacQueue，不进 hospitalQueue
  dispatchShipments(state);                 // 7. 按规则发出往返；人员占去程，占用可调度运力
  generateDailyReport(state);               // 8. 生成战报（战斗事实 + 已公开请求的满足上下文）
  refreshInfoSnapshot(state);               // 9. 更新玩家可见的延迟快照与配额预告
  ~~if (state.day >= GAME_LENGTH_DAYS) state.ended = true;~~
  evaluateCampaign(state);                  // 突破/建制失败立即结束；D30 按损失评价
}
```

以下每个函数只写"要做什么、读什么字段、写什么字段"，具体表达式全部引用第五节常量，不要在函数体里出现裸数字。

### 2. `settleQuota(state)`
- 若 `state.day === state.quota.nextArrivalDay`：
  - `arrival = QUOTA_PER_PERIOD - state.quota.pendingAdvanceDebt * QUOTA_ADVANCE_INTEREST`（利息在到货时扣，扣完清零 `pendingAdvanceDebt`，且 `arrival` 不得小于 0）；
  - `state.base.ammo += arrival * QUOTA_COMPOSITION.ammo; state.base.supply += arrival * QUOTA_COMPOSITION.supply;`
  - `state.quota.nextArrivalDay += QUOTA_PERIOD_DAYS;`
  - `state.quota.nextAmount = null`（下一批数字要到提前期才揭示，见 `refreshInfoSnapshot`）。
- 预支：若 `state.orders.advanceQuota > 0`，检查 `advanceQuota <= QUOTA_PER_PERIOD * QUOTA_ADVANCE_MAX_RATIO - state.quota.pendingAdvanceDebt`，通过则立即把 `advanceQuota` 加进 `state.base`（按 `QUOTA_COMPOSITION` 拆分），并累加进 `pendingAdvanceDebt`；不通过则该指令本回合被拒绝并记进 `eventLog`（玩家指令永远不能静默失败）。

### 3. `settleMobilization(state)`
- ~~若 `state.day === state.personnel.mobilization.nextArrivalDay`：`state.personnel.pool += state.personnel.mobilization.nextAmount`（数量数值起点未给具体每批数字，暂定 = `DIVISION_SAFE_RESERVE_DAYS` 换算出的满编点数的一个固定比例，实现时先给一个占位数，标记 TODO，交给试玩调），`nextArrivalDay += MOBILIZATION_INTERVAL_DAYS`。~~
- **修正（2026-09-17）**：若到期，动员的 `MOBILIZATION_BATCH_SIZE = 30` 点先按当日 `trainingTrack` 入 `trainingQueue`，完成训练后才进入 `pool`；`nextArrivalDay += MOBILIZATION_INTERVAL_DAYS`。原因：人员环路的既定顺序是“国家动员 → 训练 → 自由分配”，直接入池会绕过训练档位。影响范围：`settleMobilization`、`advanceQueues`、人员环路测试。

### 4. `advanceQueues(state)`
- `transitQueue`：每批 `arrivesDay === state.day` 的，按 `lineId` 的 `via` 生成一条战报文案（"车队经过中转站…"，纯文案不影响数值），然后：若目标师所在防区已丢失/战线后退到该批次送不到（demo 简化：只在 `resolveBattles` 判定"战线后退"事件时，把当天所有仍在途且目标是该师的批次标记 `lost = true`，在这一步遇到 `lost` 批次直接从队列移除、计入彻底损失、写日志），否则把 `cargo` 加进对应师的本地储备（见下"到货换算"）；从队列移除。
- `trainingQueue` / `hospitalQueue`：同样按 `arrivesDay` 结算，训练完的进入"待分配的人员池"（不是自动回原师，见机制库「人员的独立环路」——回收老兵不自动归队）；医院超过 `HOSPITAL_DAILY_QUOTA_RATIO` 换算出的当日名额时，超出部分 `arrivesDay += 1` 并标记 `overflowDays += 1`，`overflowDays > 0` 的那部分按 `HOSPITAL_OVERFLOW_DEATH_MULT` 结算死亡（从队列里扣除相应人数，计入彻底损失）。

**修正（2026-09-18）**：去程批次抵达后：卸载弹药/给养/补员（补员入该师 `integrationQueue`），立即按 `returnPriority` 从该师 `evacQueue` 装回程，没有载荷也空驶返回；车辆在回程抵达基地前不可调度。回程抵达后：伤员才写入 `hospitalQueue`，轮换进入休整（demo 可暂用短时 `trainingQueue` 同类延迟，不新开系统），车辆加回可调度运力。`hospitalQueue` 禁止由战斗直接写入。待后送超过时间窗仍未上车则彻底损失。原因：循环的铰链就是这两段装卸。影响范围：`advanceQueues`、`dispatchShipments`、可调度运力。

**到货换算**：一批 `cargo.ammo`（单位 D）到某师后，`division.ammoDays += cargo.ammo / dailyConsumptionOf(division)`；`dailyConsumptionOf` 是"该师当前姿态下的日消耗"（见 5d），这样"天数"这个抽象单位才能在"D"和"天"之间来回换算而不引入第二套单位。**修正（2026-09-18）**：同批 `cargo.personnel` 进入该师 `integrationQueue`，不直接改 `manpowerRatio`。

### 5a. `perceive(state, division)`
- 生成 `division.perceivedSelf`：把真实的五个数（满编率、弹药天数、给养天数、疲劳、装备）按 `1 - personality.selfAwareness` 的幅度加一个偏差（demo 不用骰子，用一个确定性偏移：偏差方向由"该师上一次决策是否判断正确"决定，或最简单的暂定实现——固定偏移 `trueValue * (1 - selfAwareness) * 0.3` 的正向乐观偏移，即自知力低的师总是觉得自己比实际状况好一点，与机制库"不是撒谎，是误判"一致）。这是本文件的暂定实现，标记见十一。

### 5b. `decidePosture(state, division)`
严格照抄 `机制库.md` 系统 5 的规则表，逐条判断，命中即返回，不要重排顺序：

```
若 弹药 < 2 且 未接敌            → dormant
若 弹药 < 1 且 接敌              → retreat（除非收到死守命令）
若 疲劳 > 阈值                   → 申请 refit（阈值暂定 0.75，试跑调）
若 有进攻命令 且 储备 ≥ 门槛     → attack（门槛 = 该师 aggression 换算出的天数，见下）
若 有进攻命令 但 储备不足        → 按 obedience 决定：obedience 高→硬上 attack；低→拖延（本回合仍是上一姿态）
若 储备充裕 且 判断敌方薄弱      → 按 aggression 判断自作主张 attack（demo 简化："判断敌方薄弱"＝固定小概率但不用骰子，改成一个确定性条件：连续 2 天未接敌且储备 ≥ 安全线的 1.5 倍）
否则                             → defend
```
用的是 `division.perceivedSelf`（他自己以为的状态），不是真实状态——这一条决定了"误判"能不能生效，别写错成读真实值。

### 5c. `generateRequest(state, division)`
- `baseNeed = dailyConsumptionOf(division) * 若干天缓冲`；`inflatedNeed = baseNeed * (1 + hoarding) * (共用运力时的抢运力加成)`。抢运力加成的具体实现：demo 简化为——若同一运力池上还有其他师，`inflatedNeed *= 1 + hoarding * 0.3`（"共用运力"的效果就是这个加成会挤占 `dispatchShipments` 里的优先级计算，不需要单独建模"抢"这个动作，见步骤 7）。
- 这份请求只用于 UI 展示"该师申报的需求 vs 真实需求"的落差（供玄家判断"谁在虚报"），不直接决定发多少——发多少由步骤 7 的玩家指令决定。**这是"只能改变处境，不能替他决策"的落地点：请求只是信息，不是指令。**

### 5d. `consumeAndApplyPosture(state, division)`
- `mult = POSTURE_CONSUMPTION_MULT[division.posture]`；
- `ammoConsumed = mult.ammo * ammoTierOf(division).combatMult`（**注意**：弹药消耗本身也要乘弹药档位——"部队打不出它没有的炮弹"，档位低时消耗和战斗力一起降，见机制库系统 4）；
- `supplyConsumed = mult.supply`（给养消耗不受弹药档位影响）；
- ~~若当天有交战（由 `resolveBattles` 提前标记 `division.underAttack`，注意步骤顺序：5d 在 6 之前跑，所以"是否接敌"要用**上一天**留下的 `underAttack` 状态，或者把交战判定拆成"预告 + 结算"两次，demo 简化为用"上一天"的标记，这一处顺序上的先后关系是本文件对九步顺序的必要澄清，不算改动设计）：额外加 `SPIKE_EXTRA_DAYS_EQUIVALENT` 等效的尖峰消耗；~~
- **修正（2026-09-17）**：M6 在每日消耗前根据 `battlePreview.day === state.day` 激活 `underAttack`，使尖峰消耗与同日战斗结算对应；结算后状态保留供 UI 展示，次日若无新战斗会清除。原因：避免预告日与实际交战日错开一日。影响范围：`advanceDay`、`activateScheduledBattles`、`resolveBattles` 与战斗高亮。
- `division.ammoDays -= ammoConsumed / DIVISION_SAFE_RESERVE_DAYS`（换算见常量表说明）；`division.supplyDays` 同理；
- 给养连续低于阶梯下限则疲劳与非战斗减员按机制库「给养 ↔ 人员正反馈」累加（`fatigue += x`，`manpowerRatio -= y`，`y` 部分直接计入彻底损失，不进回流）。
- `fatigue` 按姿态在 `refit` 时快速下降，其余姿态缓慢上升，具体曲线数值起点未给，暂定线性，标记见十一。

### 5d'. `autoAllocateArrivedCargo` / 步骤 6 里的战斗减员分流、`dispatchShipments`
这两块直接对应机制库系统 5「前线自行分配」与系统 4「黑箱」，实现要点：
- **前线自行分配**：本师收到的弹药，若师姿态是 `attack` 且 `hoarding` 低，允许把一部分"给养余量"挪用为弹药的等效消耗抵扣（demo 简化实现，制造"送去的东西被以你预料之外的方式花掉"的体验）——**这条如果第一版实现起来复杂度过高，可以先跳过，只保留文案（战报里写"部分给养被挪用支援攻势"），下一版再接数值**。跳与不跳都要在 `eventLog` 里显式写清楚，不要悄悄丢机制。
- **战斗黑箱** `resolveBattles(state)`：只在 `underAttack` 为真（由 `battlePreview` 提前 3 天标记）的师上跑。规则：双方效能比决定战线移动方向/距离/双方损失，效能公式见第五节。产出：`division.frontMovement`（demo 只需要一个标量：正数=前进，负数=后退，不需要真的挪动地图坐标，UI 上用文字描述"战线后退 X 公里"即可，因为界面不是地图是监护仪，机制库「摄像机跟着物资走」）；`casualties`（按 `CASUALTY_SPLIT` 分 `kia`/`wounded`，~~`wounded` 进 `hospitalQueue`~~ **修正（2026-09-18）**：`wounded` 进该师 `evacQueue`，`kia` 直接从 `manpowerRatio` 扣除且不进任何队列。医院入账只发生在回程卸载。原因：战斗直写医院会让后送变成独立入账。影响范围：`resolveBattles`、`sendWoundedToHospital` 现有调用、M5 验收）。
- **少用或不用骰子**：效能比到"移动多少/损失多少"的映射用一个确定性的分段函数（比如效能比 ≥1.5 → 大胜；1.0~1.5 → 小胜；0.7~1.0 → 小败；<0.7 → 大败，各档对应固定的移动距离与损失比例区间的**中值**，不掷骰子取具体值），符合尺子 4。

### 8. `generateDailyReport(state)`
- 每个有事件（交战/姿态变化/批次到达/queue overflow）的师生成一段文字，模板抄 `机制库.md` 系统 4 的战报示例句式（"第三师于 K12 遭遇进攻。弹药于交战第二日耗尽…"）。
- ~~**反事实提示**的实现：把“如果把某一天发给师 A 的批次改发给师 B”应用到快照后重跑结算，在失利后自动生成最大差异。~~
- **修正（2026-09-17）**：`frontRequests` 记录前线公开请求；车队在公开日至战斗日到达时累加 `deliveredAmmo`。`generateDailyReport` 只输出战斗事实和申请/到货/差额，不下决策对错结论。原因：自动最大差异忽略另一方的机会成本，会把正常取舍误报为操作错误。影响范围：`advanceQueues`、`resolveBattles`、`generateDailyReport`、请求 UI 与验收测试。

### 9. `refreshInfoSnapshot(state)`
- 按 `INFO_DELAY_DAYS` 把 `state.day - delay` 那天的师状态拷贝一份存进 `division.lastReportSnapshot`，UI 只读这份快照，不读实时状态（除了"基地库存"字段——数值起点明确写"基地库存对玩家完全透明、精确、无延迟"，这是唯一不走延迟快照的字段）。
- ~~更新配额预告：`state.day + QUOTA_PREVIEW_LEAD_DAYS === state.quota.nextArrivalDay` 时把 `nextAmount` 从 `null` 换成真实值。~~
- **修正（2026-09-17）**：当 `state.day + QUOTA_PREVIEW_LEAD_DAYS >= state.quota.nextArrivalDay` 时，`nextAmount` 从 `null` 换成精确值，并持续到该批配额结算；到货后重置为下一批的隐藏状态。原因：预告一旦公开，应在剩余提前期内持续可见，而不是只在窗口开启当天短暂可见。影响范围：`refreshInfoSnapshot`、M7 验收测试与后续配额 UI。

---

## 七、玩家指令 → UI 收集流程

`Demo 切割线.md` 三、命令：玩家每天能做的只有五件事，全部写进 `state.orders`，UI 侧不需要"确认发车"按钮——**没有手动发车**，玩家改的是规则，`dispatchShipments` 每天自动按规则执行：

| 命令 | 对应 `orders` 字段 | UI 控件（暂定） |
| --- | --- | --- |
| ~~1. 调整线路配比~~ | ~~`lineRatios`~~ | ~~每条线三个滑条（弹药/给养/人员），归一化到合计 1~~ |
| ~~1. 调整线路物资配比（2026-09-17 修正）~~ | ~~`lineRatios`~~ | ~~每条线两个滑条（弹药/给养），归一化到合计 1；人员在独立人员管线中展示和分配~~ |
| 1. 派可调度车队 + 去程构成（2026-09-18 修正） | `divisionPriority`、`lineRatios` | 各方向派车量（可暂映射原份额加减）+ 每条线三个滑条（弹药/给养/补员），归一化到合计 1 |
| 2. 人员规则 + 训练档位 + 回程规则 | `trainingTrack`、`personnelAssignment`、`returnPriority` | 训练档位单选 + 去程人员份额 + 回程默认伤员优先、可改为轮换优先；无回程确认按钮 |
| 3. 设置最低储备线 | `minReserveDays` | 每师一个数字输入（天） |
| 4. 回电告知真相 | `briefTruth` | 每师一个勾选框 |
| 5. 今日派车上限 + 是否预支 + 下令轮换 | `dailyShipmentCap`、`advanceQuota`、`rotationOrder` | 一个数字输入（默认 = 当前可调度运力）+ 预支数量 + 轮换人数；装载工兵无控件 |

**每天一次"推进"按钮**调用 `advanceDay(state)`，之前玩家可以随意改 `orders` 里的值，改了立即体现在下一次推进（不需要"提交"这个中间态，`orders` 本身就是当前生效的规则，直到玩家再改）。

---

## 八、监护仪面板字段映射（`logistics-ui.js` 要渲染的东西）

对应 `Demo 切割线.md` 三、界面：

- **首屏信息层级（2026-09-17 补充）**：命令控件之前先显示“后方库存 → 在途物资 → 前线缺口”，汇总 `base`、`transitQueue` 与各师相对最低储备线的缺口；紧接着显示“训练中 → 人员池 → 整编中”及医院人数。原因：玩家必须先理解存量与流量，才能判断如何分配。影响范围：`logistics-map-ui.js` 的首屏布局与 M8 UI 验收。
- ~~首屏以“后方库存 → 在途物资 → 前线缺口”三个汇总卡作为主要调度信息。~~ **修正（2026-09-17）**：主要调度信息改为“今日调度台”，明确显示基地弹药/给养与合计、全线日运力与玩家上限、当前命令实际计划量，并逐师显示预计载荷与 ETA。UI 通过无副作用的 `planConfiguredShipments(state)` 复用正式发运算法，`dispatchConfiguredShipments` 执行同一份计划，避免预览与结算静默分叉。原因：玩家必须无需心算即可回答“有多少、能发多少、发给谁”。影响范围：`logistics-core.js`、`logistics-map-ui.js`、M8 发运预览与 UI 合同测试；结算规则不变。
- ~~“今日调度台”逐师使用数字输入编辑优先级，在途进度主要由地图车队点承载。~~ **修正（2026-09-17）**：改为“今日发运单”，用 `data-priority-step` 加/减操作映射到现有 `divisionPriority`，每次操作重算 `planConfiguredShipments(state)` 并直接回显载荷；另遍历 `transitQueue` 渲染“全部在途运输”，进度为 `(state.day - dispatchedDay) / (arrivesDay - dispatchedDay)`，同时显示剩余天数与 ETA。原因：玩家操作的对象应是发运份额而非抽象权重，且必须能逐批核对所有在途物资。影响范围：`logistics-map-ui.js`、`logistics-map-demo.html`、M8 UI 验收；核心结算逻辑不变。
- ~~首屏操作对象为“今日发运单”。~~ **修正（2026-09-18）**：首屏改为“可调度车队”；`data-priority-step` 文案改为派车量，底层仍可暂映射 `divisionPriority`。回程预计装载只读，按 `returnPriority` 自动发生，无确认控件，无装载工兵控件。原因：操作对象必须等于往返中的车。影响范围：`logistics-map-ui.js`、发运面板文案、M8 UI 合同；核心结算逻辑可暂不改结构。
- **修正（2026-09-18）**：核心结算已改：`planConfiguredShipments` 按 `availableTransport` 和去程构成（含补员）生成带 `fleetSize` 的往返预览；抵达后 `advanceQueues` 自动装回程。原因：只改文案会让操作对象与结算再次分叉。影响范围：`logistics-core.js`、可调度运力、M8 规则发运测试。
- ~~地图节点仅显示无标签的百分号，路线弹窗仅标“弹药/给养占比”。~~ **修正（2026-09-17）**：节点百分号明确标为“效能”，即满编率、弹药档位、装备完好率、疲劳与整编惩罚的综合结果；路线百分号明确标为“本日待发车队”的弹药/给养装载构成。修改路线构成只影响下一次 `dispatchConfiguredShipments` 生成的批次，不改变 `divisionPriority` 所决定的发运份额、周期配额或任何 `transitQueue` 中已在途批次；“前线公开请求”的申请量明确是战前弹药目标（D），不是配额或百分比。原因：无标签百分号和“占比”会把前线效能、路线装载、配额及在途进度混为同一可编辑数值。影响范围：地图节点、路线弹窗、公开请求说明与 M8 UI 合同测试；核心结算逻辑不变。
- ~~前线节点只显示综合效能结果，不提供计算依据。~~ **修正（2026-09-17）**：悬停节点显示“满编率 × 弹药档位 × 装备完好率 × 抗疲劳 × 整编系数”的公式摘要；点开师的面板逐项列出报告快照中的数值、各乘数和结果，并显示报告日及滞后天数。原因：效能是重要反馈，但不应是不可审计的黑箱；延迟情报也必须与其计算依据同时可见。影响范围：`logistics-map-ui.js`、`logistics-map-demo.html` 与 M8 UI 合同测试；`computeEffectiveness` 结算公式不变。
- ~~发运单与在途清单均常驻于地图上方。~~ **修正（2026-09-17）**：增加纯展示层状态 `dispatchPanelOpen`，默认 `false`；`renderFlowOverview()` 在收起时只返回单行 `dispatch-summary-bar`，展开时才返回完整发运单。`renderTransitBoard()` 改在 `#dock .transit-dock` 内常驻渲染。原因：地图是主操作面，调度编辑器不应长期占用其垂直空间；在途监控仍需随时可见。影响范围：`logistics-map-ui.js`、`logistics-map-demo.html`与地图首屏验收。
- 一行一个师：`效能条`（读 `computeEffectiveness(division)`，见第五节公式）、`三种物资剩余天数`（`ammoDays`/`supplyDays`/`manpowerRatio` 换算）+ 下降箭头（跟上一次快照比较）、`该行数据的时间戳`（`division.lastReportSnapshot.day`）。
- 交战中的师高亮：读 `division.underAttack`。
- 在途车队：遍历 `transitQueue`，显示 `lineId`、`cargo`、`arrivesDay`。
- **必须有的一行**：`第X师弹药预计 D1 耗尽。第 N 批预计 D2 抵达。`——`D1 = state.day + division.ammoDays`（按当前消耗速率线性外推），`D2` 取该师在途队列里最近一批的 `arrivesDay`。
- **后方对应行**：`基地弹药按当前发运速度可支撑至 D3。下批配额 D4 到货。`——`D3` 按 `base.ammo / 当前日均发运量` 外推，`D4 = quota.nextArrivalDay`。
- 每日战报区：读 `eventLog`，最新一条置顶。

---

## 九、里程碑与实现顺序

每条里程碑给出：目标、涉及文件、这一步之后必须能做到的事（验收标准）。**做不到验收标准不要进入下一条**，哪怕代码已经"写完"。

### M0 · 骨架
- 建好第二节列的五个文件，`logistics-core.js` 导出一个空的 `createInitialState()` 返回第四节的结构（数值全部取自第五节常量），`logistics-demo.html` 能在 `npm run dev` 下打开并显示一句"世界已创建，第 0 天"。
- 验收：能跑 `node logistics-core.test.js`（哪怕只有一个 `assert.ok(createInitialState().day === 0)`）。

### M1 · 运力与在途队列（不含师、不含战斗）
- 实现 `dispatchShipments`（步骤 7）与 `advanceQueues`（步骤 4）中的 `transitQueue` 部分。先不管消耗、不管配额，基地库存先给一个固定大数模拟"无限"，只验证"发出去的东西会按线路的 `transitDays` 延迟到达"。
- 验收：写一个测试——第 1 天发一批货到 `div-1`，断言它在 `day + transitDays` 那天出现在师的本地库存里，不早不晚。

### M2 · 有限存量 + 周期配额（系统 0 落地）
- 接入 `settleQuota`，基地库存从"大数"换成 `INITIAL_BASE_INVENTORY`，按第五节公式跑配额到货、预支、利息。
- ~~验收：写一个测试模拟"零发运"30 天，断言 `base.ammo`/`base.supply` 按 `Demo 切割线.md` 给出的公式 `库存(t) = 8 − t + 6 × floor(t/7)` 走（这条公式是设计文档自己给的，直接拿来当测试断言，不用重新推导）。~~
- **修正（2026-09-17）**：零发运不会产生每日 `−1 D` 的出库，30 天测试应断言总库存按 `库存(t) = 8 + 6 × floor(t / 7)` 走，并按固定的弹药/给养构成拆分。原公式描述的是"每天实际发运并消耗 1 D"的满负荷情景，不适用于零发运测试；该情景可在 M3 接入消耗后另设测试。

### M3 · 师的状态数 + 消耗三层叠加 + 阶梯短缺
- 实现 `consumeAndApplyPosture`、`AMMO_TIER` 查表、效能公式。姿态先固定为 `defend`（师 AI 还没接入），只验证"给定姿态，消耗按倍率走，储备天数按阶梯影响效能"。
- 验收：三个师全程 `defend`，用数值起点里"三个师全程积极防御"的总需求公式反过来对拍（把 `defend` 换成 `active-defend` 测试更贴合起点公式），断言总消耗与 `Demo 切割线.md` 给出的 30D 数量级一致（允许合理误差，因为该文档的公式本身是估算）。

### M4 · 师的 AI（姿态状态机 + 性格四参数）
- 接入 `perceive` + `decidePosture` + 三套性格预设。
- 验收：三个师分别赋 `aggressive`/`hoarder`/`obedient` 预设，跑一段固定剧本（比如同样的稀缺条件），断言三个师选出的姿态序列不同，且符合各自人设的方向性（冲锋型比老实人更早进入 `attack`，铁公鸡在同等库存下囤积倾向让他更晚进入 `dormant`）。

### M5 · 人员独立环路
- 接入 `settleMobilization`、`trainingQueue`、`hospitalQueue`、`CASUALTY_SPLIT`、`MEDIC_RECOVERY_RATE`、建制消化阶梯。
- ~~验收：模拟一批负伤，断言最终有 `MEDIC_RECOVERY_RATE` 比例的人回到 `personnel.pool` 且标记为"免训练"，其余按医院顺延规则算出彻底损失人数。~~
- **修正（2026-09-18）**：验收改为两段。无回程抵达时，负伤只停留在 `evacQueue`，`hospitalQueue` 必须仍为空。有回程卸载后，医院才按 `MEDIC_RECOVERY_RATE` 回收到 `personnel.pool` 并标记免训练；超额部分仍按医院顺延死亡。人员从 `pool` 上前线必须出现在去程 `cargo.personnel`。测试名称：`round-trip personnel cycle tests`。原因：旧验收允许战斗直写医院。影响范围：M5 测试与 `sendWoundedToHospital`。

### M6 · 战斗黑箱 + 战报归因
- ~~接入 `generateDailyReport` 的无条件反事实，并用“若多留 X 天储备”验收损失方向。~~
- **修正（2026-09-17）**：接入 `resolveBattles`、`generateDailyReport`、`battlePreview` 与 `frontRequests`。验收构造“已公开申请 0.6 D、窗口内到达 0.2 D”的场景，断言战报包含弹药耗尽、战线/伤亡/装备的持久变化及申请/到货/差额，并断言不出现“若改发”类训诫。原因：验收应覆盖可审计的当时已知信息，而非事后最优解。影响范围：M6 测试名称、战报事件类型与 M8.5 公平归因验收。

### M7 · 信息延迟与预告
- 接入 `refreshInfoSnapshot`、配额预告分级、`INFO_DELAY_DAYS`。
- 验收：修改真实状态后，UI 读到的快照要落后 `INFO_DELAY_DAYS` 天才更新；配额预告在提前期之前必须是 `null`/模糊，之后必须是精确值。

### M8 · UI 接完整一轮
- ~~`logistics-ui.js` 按第七、八节把五项指令的输入控件和监护仪面板接上，能连续点"推进"跑完 30 天不崩。~~
- `logistics-ui.js` / `logistics-map-ui.js` 按第七、八节接上五项指令，并在命令之前展示物资与人员流动总览；能连续点“推进”跑完 30 天不崩。（2026-09-17 修正：地图交互原型已成为主要验证入口。）
- ~~验收：手动玩一局，30 天跑完不报错，`eventLog` 里能读出一条完整的故事线。~~
- ~~验收：`transit-first information hierarchy contract tests` 断言首屏含“后勤流动总览 / 在途物资 / 人员流转”。~~ ~~`dispatch-readability information hierarchy contract tests` 改为断言“今日调度台 / 我有多少物资 / 今天最多能发多少 / 今天预计装载”。~~ **修正（2026-09-17）**：`dispatch-sheet and transit-progress contract tests` 断言“今日发运单 / 可用库存 / 本日实际装载 / 加减份额 / 全部在途运输 / 剩余天数”，并保留发运预览纯函数与人员线路隔离断言。原因：验收应覆盖新交互和逐批运输进度。影响范围：`logistics-core.test.js` 的 UI 合同段。

**修正（2026-09-18）**：作废“人员线路隔离断言”。发运预览必须含去程人员与回程预计载荷；在途清单必须能区分去程/回程。测试名称改为 `round-trip dispatch and transit-progress contract tests`。原因：隔离断言在固化两本账。影响范围：`logistics-core.test.js` UI 合同段。

**修正（2026-09-18）**：合同文案从“今日发运单 / 加减份额 / 打开发运单”改为“可调度车队 / 派车 / 回程预计装载”。断言不得出现回程确认按钮或装载工兵控件。原因：操作对象与回程规则已定案。影响范围：`logistics-core.test.js` UI 合同段、`logistics-map-ui.js`。
- ~~`dispatch-sheet and transit-progress contract tests` 只验证发运单和进度清单存在。~~ **修正（2026-09-17）**：验收名称改为 `map-first dispatch and transit-progress contract tests`，额外断言“发运摘要 / 打开发运单 / transit-dock”，保证默认地图优先布局与在途监控常驻。影响范围：`logistics-core.test.js` UI 合同段。

### M8.5 · 压力剧本、持久战线与公平归因（2026-09-17 修正）

- ~~D30 只把 `ended` 设为 `true`，战线移动只保留当次数值，战报对失利自动给出“若多留 2 天弹药”。~~
- **修正（2026-09-17）**：新增确定性 `CAMPAIGN_BATTLE_SCRIPT`、`frontPosition`、装备损失、建制/战线失败阈值和 `campaign.result`；前线战前请求按“公开日—战斗日”窗口累计实际到货。战报只陈述请求与到货差额，不自动评判决策对错，也不生成没有完整机会成本的反事实。原因：玩家只应对当时可知、可控的选择承担责任；正常取舍不应被事后标准答案训诫。影响范围：`logistics-constants.js`、`logistics-core.js`、`logistics-map-ui.js`、`logistics-map-demo.html`、M6/M8.5 测试与终局 UI。
- 验收：无调度的固定剧本必须在 D30 前战败；战线与装备损失必须跨战斗保留；有公开请求时战报显示申请/到货/差额，无公开请求时不得生成指责性反事实；D30 须给出胜利/惨胜/失败而非单纯“战役结束”。

### M9 · 对照 `Demo 切割线.md` 六、验证标准逐条自测
- 把该节的检查清单原文抄进 `logistics-core.test.js` 顶部注释，逐条标记"这一条现在能不能在 UI 上被玩家感知到"。这一步不是写断言，是拿这个原型自己玩十局，人工判断。
- 验收：第一条和第二条（及格线）命中；其余命中任意两条。命不中——回到「六、验证标准」最后一段的调参顺序（先调运力，再调流入，不要同时调），只改第五节常量表，不改结构。

**不要在 M9 之前动手实现 `Demo 切割线.md` 第二节"不进"表格里的任何一条**，包括但不限于：配额构成申报、人员四分、伤员轻重分档、装备回收、拥堵非线性、中转装卸、节点囤积风险、观察稀缺、向上级申诉、假消息、反后勤系统。这些等 M9 验证通过后，回 `机制库.md` 按需取用，那时候会是另一轮里程碑，不在本文件范围内。

---

## 十、测试计划（`logistics-core.test.js`）

跟 `campaign-core.test.js` 一样用 `node:assert/strict`，每个里程碑至少留下一条断言，不要事后补测试。建议的断言清单（对应第九节里程碑）：

1. `createInitialState()` 的字段与第四节 `GameState` 结构一致（用 `Object.keys` 粗查即可，不用逐字段深比对）。
2. 零发运 30 天，`base` 库存轨迹与 `Demo 切割线.md` 给出的公式一致。
3. 三个师全程 `active-defend`，30 天总消耗与数值起点给出的 30D 量级一致。
4. 弹药档位边界值（0.5/0.2/0.05）两侧的 `combatMult` 分别取对应两档，不要在边界上取错档。
5. 三套性格预设在同一剧本下产出不同姿态序列。
6. ~~一批负伤经过医院队列，回收比例与 `MEDIC_RECOVERY_RATE` 相符，医院超额部分按 `HOSPITAL_OVERFLOW_DEATH_MULT` 结算。~~ **修正（2026-09-18）**：无回程则伤员不得进入医院；有回程卸载后回收比例才与 `MEDIC_RECOVERY_RATE` 相符。另断言人员上前线必须走去程载荷。原因与影响范围同 M5。
7. ~~反事实重算：换一批货的接收方，重跑结算，只检查被补给一方的损失方向。~~ **修正（2026-09-17）**：公平归因测试断言公开请求的申请/到货/差额可追溯，且未能同时重算机会成本时不生成指责性反事实。原因：单边反事实必然伪造无代价正解。影响范围：第 7 项测试名称与断言。
8. 信息延迟：篡改真实状态后立即读快照，断言快照仍是延迟前的值，直到延迟天数过去。

---

## 十一、实现时的模糊点与暂定选择（供审查）

设计文档已经把"为什么"讲得很清楚，但落到代码时有几处需要一个具体数字或具体单位，原文没有给到这个精度。以下每一条都是本文件替设计文档做的补充决定，不是新机制，纯粹是"把话说到能编译的精度"。如果试玩后发现不对，只改这里、改常量表，不用回头怀疑机制本身：

1. **"底噪（固守）1.0/2.0"两个数字分别对应哪种物资** —— 暂定：给养 1.0，弹药 2.0。
2. **弹药档位百分号阶梯 → `ammoDays` 天数的换算基准** —— 暂定引入 `DIVISION_SAFE_RESERVE_DAYS = 10` 作为"100%"的基准天数。
3. **人员池、训练队列、医院队列的计数单位** —— 暂定 1 点 = 1% 满编。
4. **国家动员每批数量** —— 数值起点没给具体数字，先占位，标 TODO，交给试玩第一次调参。
5. ~~**疲劳的具体上升/恢复曲线** —— 暂定线性，各姿态一个固定斜率常量，试跑调。~~
   **修正（2026-09-17）**：M3 采用 `POSTURE_FATIGUE_CHANGE` 中的确定性线性斜率：`refit -0.12`、`defend +0.01`、`dormant -0.03`、`active-defend +0.035`、`attack +0.07`、`retreat +0.04`；每次结算钳制在 0–1。原因：M3 的消耗函数必须同步更新疲劳，原文未给出可执行的数值。影响范围：`consumeAndApplyPosture` 与后续读取 `fatigue` 的 AI/效能计算；数值仅为试玩起点。
6. **"自知力"造成的偏差方向与幅度、"判断敌方薄弱"的具体判定条件** —— 暂定用确定性规则代替随意判断（尺子 4 要求不掷骰子），具体条件见第六节 5a/5b。
7. **"前线自行分配"里给养挪用弹药的具体数值规则** —— 允许第一版只做文案不做数值，标记清楚不要悄悄丢机制。
8. ~~**效能比 → 战线移动/损失的分段函数具体区间** —— 暂定四档（大胜/小胜/小败/大败），各档给固定中值，不掷骰子。~~
   ~~**修正（2026-09-17）**：M6 以敌军效能 `0.7` 为固定基准；效能比分别在 `≥1.5 / ≥1.0 / ≥0.7 / <0.7` 时，战线变化为 `+8 / +3 / -3 / -8` 公里，己方减员率为 `2% / 4% / 8% / 15%`。反事实提示比较额外保留 2 天弹药后的减员档位。~~
   **修正（2026-09-17）**：保留上述确定性效能比和战线/减员映射，并为四档增加 `1% / 2% / 5% / 10%` 装备损失；删除额外保留 2 天弹药的单边反事实。原因：战斗结果需要持久代价，但不应用隐藏的无代价最优解评判玩家。影响范围：战斗结算表、装备完好率、战报与 M6/M8.5 测试。

9. ~~**进攻储备门槛由 aggression 换算出的天数** —— 文中写“见下”，但未给出换算。~~
   **修正（2026-09-17）**：M4 暂定 `attackReserveThreshold = 2 + (1 - aggression) × 6` 天；服从度 `≥ 0.7` 会在储备不足时强行执行当日的上级进攻命令；激进度 `≥ 0.7`、连续两天未接敌且储备达本人门槛 1.5 倍时可自主进攻。原因：姿态状态机需要确定性门槛，且 demo 不使用骰子。影响范围：`decidePosture`、`quietDays` 状态字段与 M4 固定剧本测试；均为试玩起点。

10. ~~**国家动员每批数量** —— 数值起点没给具体数字，先占位，标 TODO，交给试玩第一次调参。~~
    **修正（2026-09-17）**：M5 暂定每 21 天动员 `30` 个满编点。原因：人员环路的队列与训练档位需要一个可运行的批量；该数值不改变环路结构。影响范围：`MOBILIZATION_BATCH_SIZE`、动员结算及 M5 测试；为首次试玩的调参起点。

11. **人员点数如何占去程 D、回程占用几天** —— **新增（2026-09-18）**：暂定 10 点人员 = 0.1 D 去程载荷（`PERSONNEL_PER_D = 100`）；回程占用天数 = 该线 `transitDays`（与去程对称）。待后送超过 `transitDays + 2` 天未上车则彻底损失。原因：循环落地必须有可编译的换算，且不得新开资源条目。影响范围：`dispatchShipments`、`evacQueue`、可调度运力；均为试玩起点。
12. **轮换人员抵达后方后的休整天数** —— **新增（2026-09-18）**：暂定 `ROTATION_REST_DAYS = 3`，复用 `trainingQueue` 的 `track: 'rest'`，不新开系统。原因：设计只要求休整后再争夺去程，未给天数。影响范围：回程卸载、人员池；为试玩起点。


以上条目，任何一条被后续设计讨论明确否定或给出更精确的数字，都应该先去改 `机制库.md` / `Demo 切割线.md` / `运输与人员循环机制设想.md`，再回来同步这份文件，不要反过来先改代码再假装设计文档也是这么说的。
