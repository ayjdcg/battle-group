# 战役地图与行军系统

## 当前目标

战役地图负责连接自动战斗与未来的主城经营。玩家在地图上决定兵团投入哪条战线、前往哪个目标以及何时增援；系统负责路径选择、行军和遭遇后的自动战斗。

当前实现仍是验证原型，但地图上的可控对象已经是兵团，而不是单个作战单位。兵团内部最多保留 9 个单位槽位，单位在节点战中自动展开。

## 地图结构

地图是图结构，不是方格地图。画布坐标只负责显示，节点和通路数据才决定可达关系。

- 固定三条不规则主线：北线、中央线、南线；
- 两端各有一个司令部；
- 每条主线内部按顺序连接；
- 生成 3–4 条错位横向连接，提供增援和侧翼选择；
- 生成一条由蓝方向红方通行的单向捷径；
- 保证出现桥梁、渡河、山口及必要地形节点。

默认地图参数：

| 参数 | 当前规则 |
| --- | --- |
| `playerCorps` | 默认 6，限制为 4–8；当前主要用于决定地图规模。 |
| 节点数 | 约为 `playerCorps × 3.5 + 2`，对齐三条战线后限制为 20–32。 |
| 默认节点数 | 6 对应 23 个节点；4 对应 20 个；8 对应 32 个。 |
| 随机方式 | 相同 `seed` 与配置始终生成相同地图。 |

注意：当前 UI 中的“地图规模/兵团”输入只传给地图生成器，并不会生成对应数量的玩家兵团。蓝方当前固定使用一套默认单位配置。

## 节点数据

每个节点包含：

```js
{
  id,
  team,
  type,
  x,
  y,
  lane,
  column,
  name
}
```

节点定义额外提供 `role`、`capacity` 和 `effects`。当前已定义司令部、展开地域、城镇、城区、工业区、高地、林地、低地、湿地、隘口、交通枢纽、铁路枢纽、桥头、渡口、补给站、野战医院、前线机场、前沿指挥所、观测哨、永备工事和战役目标。

部分节点标签已经参与规则结算，其他标签仍只用于表达未来语义：

- `highland` 的 `fireAdvantage` 会为控制该节点的一方提供火力加成；
- `town`、`fortification` 等节点的 `cover`、`defend` 会降低控制方所受伤害；
- 所有节点的 `capacity` 会限制每方同时投入战斗的兵团数；
- `fieldHospital` 的治疗与后送、以及大多数侦察和维修标签尚未结算。

`campaign-core.js` 负责解释这些标签；地图生成器本身只产出数据，不负责结算。

## 通路数据

每条通路包含：

```js
{
  id,
  from,
  to,
  type,
  directed
}
```

通路类型包括道路、高速路、土路、桥梁、渡河、山口和单向通道，并提供 `moveCost` 与 `capacity` 定义。

当前规则读取连接关系、单向属性和下一段通路的移动代价：

- 双向通路可以从任意一端进入；
- 单向通路只能从 `from` 前往 `to`；
- 寻路使用无权重广度优先搜索；
- `moveCost` 会修正当前行军段的速度；
- 通路 `capacity` 尚未造成行军拥堵，仍待后续接入。

因此，通路样式反映已有的移动差异，但尚不代表完整的容量、阻断或通路遭遇规则。

## 地图生成保证

`validateMap(map)` 当前检查：

- 可控兵团配置在 4–8 范围；
- 节点总数在 20–32 范围；
- 蓝红双方司令部存在；
- 蓝方司令部能够到达红方司令部；
- 每条通路引用的节点存在；
- 高地、低地、交通枢纽、桥头和渡口存在；
- 桥梁、渡河和山口通路存在；
- 至少存在一条单向通路。

生成结果不满足这些条件时会直接抛出错误。

## 当前行军规则

1. 蓝方兵团初始位于蓝方司令部；
2. 玩家选择一个仍可行动的蓝方兵团，再点击目标节点；
3. 系统根据当前通行方向计算最短段数路径；
4. 单位按路径连续行军，不要求逐节点下令；
5. 正在交战的兵团不能接受新的移动命令；
6. 当前测试模式下，敌方在“开始战役”后先集结至敌方中央线目标，再按固定中央线节点向蓝方司令部推进；
7. 敌对单位在同一节点相遇，或在同一条通路相向行军并相遇时，自动进入战斗；
8. 通路遭遇战结束后，胜方继续原有路径。

### 战斗测试约束

地图仍保留三条战线、横向连接和单向捷径，供蓝方验证不同兵团编组与增援路线；但红方暂不使用这些侧翼选择。这样每次测试的敌军压力都来自中路，避免 AI 从边线直达蓝方司令部而干扰编制与战斗数据的对比。这个约束属于当前测试规则，而非地图生成器的永久通行限制。

## 当前战斗与胜负

当前节点战使用逐单位的自动战斗规则。单位按攻击冷却持续行动；战斗开始时系统按职责自动展开，不让玩家手动摆放九宫格。

- 坦克、突击步兵进入前列；机枪组、榴弹兵与反坦克班进入支援列；医疗兵与防空班进入后卫；攻击直升机进入空中层；
- 地面单位只能优先攻击敌方最前方仍有单位的地面层，因此前列会掩护支援列与后卫；
- 医疗兵跨层治疗生命比例最低的友军；防空班只攻击直升机；直升机优先攻击坦克；
- 战斗窗口使用和规则层相同的自动成型数据：橙线表示攻击、绿虚线表示治疗，线宽表示本次作用强度；
- 当前为无补给基线。伤害按 65% 缩放、冷却按 1.8 倍延长，以保证单位的目标选择和克制关系可读。

蓝方胜利：

- 任意存活蓝方单位占领红方战役目标，且该处已无敌人。

蓝方失败：

- 任意存活红方单位进入蓝方司令部并清除当地敌人；
- 所有蓝方单位损失。

当前界面的失败文案只显示“蓝方单位全损”，与红军进入司令部这一失败条件并不完全对应，后续 UI 应统一。

## 调用方式

浏览器中加载 `map-generator.js`：

```js
const map = CampaignMapGenerator.generateMap({
  playerCorps: 6,
  seed: 'operation-red-river'
});
```

CommonJS：

```js
const { generateMap, validateMap } = require('./map-generator.js');
const map = generateMap({ playerCorps: 6, seed: 'operation-red-river' });
```

主要结果位于：

```js
map.nodes
map.edges
map.definitions
map.validation
```

`map-generator-demo.html` 只用于预览地图生成结果；完整的行军和战斗入口是 `index.html`。

## 下一步

地图系统接下来按以下顺序完善：

1. 打磨自动成型、前列掩护与兵种克制的数值；
2. 完善通路遭遇战与通路容量造成的拥堵；
3. 将更多地形、防御、维修和侦察效果接入战斗；
4. 明确节点控制权变化与多个战役目标的胜利规则；
5. 保存兵团战损，为未来的主城补充系统提供输入。
