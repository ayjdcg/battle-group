# 战役地图与行军系统

## 当前目标

战役地图负责连接自动战斗与未来的主城经营。玩家在地图上决定兵团投入哪条战线、前往哪个目标以及何时增援；系统负责路径选择、行军和遭遇后的自动战斗。

当前实现仍是验证原型：地图上的可控对象暂时是单个作战单位，而不是正式兵团。

## 地图结构

地图是图结构，不是方格地图。画布坐标只负责显示，节点和通路数据才决定可达关系。

- 固定三条不规则主线：北线、中央线、南线；
- 两端各有一个司令部；
- 每条主线内部按顺序连接；
- 生成 3–4 条错位横向连接，提供增援和侧翼选择；
- 生成一条由蓝方向红方通行的单向捷径；
- 保证出现桥梁、渡河、山口及必要地形节点。

默认地图参数：

| 参数 | 当前规则 |
| --- | --- |
| `playerCorps` | 默认 6，限制为 4–8；当前主要用于决定地图规模。 |
| 节点数 | 约为 `playerCorps × 3.5 + 2`，对齐三条战线后限制为 20–32。 |
| 默认节点数 | 6 对应 23 个节点；4 对应 20 个；8 对应 32 个。 |
| 随机方式 | 相同 `seed` 与配置始终生成相同地图。 |

注意：当前 UI 中的“地图规模/兵团”输入只传给地图生成器，并不会生成对应数量的玩家兵团。蓝方当前固定使用一套默认单位配置。

## 节点数据

每个节点包含：

```js
{
  id,
  team,
  type,
  x,
  y,
  lane,
  column,
  name
}
```

节点定义额外提供 `role`、`capacity` 和 `effects`。当前已定义司令部、展开地域、城镇、城区、工业区、高地、林地、低地、湿地、隘口、交通枢纽、铁路枢纽、桥头、渡口、补给站、野战医院、前线机场、前沿指挥所、观测哨、永备工事和战役目标。

这些标签已经生成并展示，但尚未参与规则结算。例如：

- `highland` 带有观察和火力优势标签；
- `fieldHospital` 带有治疗与后送标签；
- `fortification` 带有防守、掩体和缩窄战线标签；
- `objective` 带有胜利与控制标签。

未来应由 `campaign-core.js` 将标签解释为移动、战斗、补给或恢复效果，地图生成器本身不负责结算。

## 通路数据

每条通路包含：

```js
{
  id,
  from,
  to,
  type,
  directed
}
```

通路类型包括道路、高速路、土路、桥梁、渡河、山口和单向通道，并提供 `moveCost` 与 `capacity` 定义。

当前规则只读取连接关系和单向属性：

- 双向通路可以从任意一端进入；
- 单向通路只能从 `from` 前往 `to`；
- 寻路使用无权重广度优先搜索；
- `moveCost` 和 `capacity` 尚未影响路径或速度；
- 所有通路上的基础移动速度相同。

因此，界面上不同通路的样式目前主要用于表达未来语义，不代表已经存在完整数值差异。

## 地图生成保证

`validateMap(map)` 当前检查：

- 可控兵团配置在 4–8 范围；
- 节点总数在 20–32 范围；
- 蓝红双方司令部存在；
- 蓝方司令部能够到达红方司令部；
- 每条通路引用的节点存在；
- 高地、低地、交通枢纽、桥头和渡口存在；
- 桥梁、渡河和山口通路存在；
- 至少存在一条单向通路。

生成结果不满足这些条件时会直接抛出错误。

## 当前行军规则

1. 蓝方单位初始位于蓝方司令部；
2. 玩家选择一个仍可行动的蓝方单位，再点击目标节点；
3. 系统根据当前通行方向计算最短段数路径；
4. 单位按路径连续行军，不要求逐节点下令；
5. 正在交战的单位不能接受新的移动命令；
6. 敌方在“开始战役”后自动寻找前往蓝方司令部的路径；
7. 敌对单位在同一节点相遇，或在同一条通路相向行军并相遇时，自动进入战斗；
8. 通路遭遇战结束后，胜方继续原有路径。

## 当前战斗与胜负

节点战和通路遭遇战使用相同的简化自动战斗规则。单位按攻击冷却持续行动，自动选择当前能够造成最高伤害的敌人；医疗兵治疗同场生命比例最低的友军。

蓝方胜利：

- 任意存活蓝方单位占领红方战役目标，且该处已无敌人。

蓝方失败：

- 任意存活红方单位进入蓝方司令部并清除当地敌人；
- 所有蓝方单位损失。

当前界面的失败文案只显示“蓝方单位全损”，与红军进入司令部这一失败条件并不完全对应，后续 UI 应统一。

## 调用方式

浏览器中加载 `map-generator.js`：

```js
const map = CampaignMapGenerator.generateMap({
  playerCorps: 6,
  seed: 'operation-red-river'
});
```

CommonJS：

```js
const { generateMap, validateMap } = require('./map-generator.js');
const map = generateMap({ playerCorps: 6, seed: 'operation-red-river' });
```

主要结果位于：

```js
map.nodes
map.edges
map.definitions
map.validation
```

`map-generator-demo.html` 只用于预览地图生成结果；完整的行军和战斗入口是 `index.html`。

## 下一步

地图系统接下来按以下顺序完善：

1. 用真正的兵团替换可单独下令的作战单位；
2. 让兵团保留目的地、路径、驻守和撤退状态；
3. 将通路 `moveCost` 接入加权寻路与行军速度；
4. 将节点和通路 `capacity` 接入拥堵或战斗宽度；
5. 接入地形、防御、补给、维修和侦察效果；
6. 明确节点控制权变化与多个战役目标的胜利规则；
7. 保存兵团战损，为主城补充系统提供输入。
