# 战术指令系统：当前设计共识

## 目标

游戏不是 RTS：玩家不逐个选择单位、不点击单位移动、不要求持续微操。

战斗仍以自主 AI 为主体，但玩家能以少量高层战术意图影响战局。体验目标是“布置、预测、纠偏”，而非操作手速。

当前先验证区域指令本身是否有趣；**暂不实现战斗组**。战斗组是将来限定指令作用对象、提供任务上下文的一层，不应抢在区域和行为系统验证之前设计。

## 三层概念必须分离

```text
区域：战场上的语义地点与规则
行为：单位实际执行的动作方式
指令：将某种意图或行为倾向绑定到区域、目标或条件
```

### 区域

区域描述“这里是什么”，不直接等同于一个动作。它至少有位置、范围、阵营归属、语义标签、优先级和状态。

当前原型的区域：

| 区域 | 语义 | 当前可见作用 |
| --- | --- | --- |
| 集结区 | 整队、准备行动的锚点 | 远距离交战前，地面单位优先靠拢。 |
| 压制区 | 希望被压住、难以通行的敌方地带 | 机枪组、榴弹兵、坦克优先选择区内目标。 |
| 火力覆盖区 | 需要持续重点打击、同时有友伤风险的地带 | 榴弹兵、坦克优先炮击区内目标；其他地面单位主动避开。 |
| 危险区 | 应被视为高风险的地带 | 我方地面单位离开该区域。 |

后续可扩展：防卫区、撤退区、伏击区、补给/救援区、禁入区、推进扇区。

### 行为

行为是 AI 根据自身能力与局势执行的方式，例如：接近、远离、停驻、巡逻、压制、攻击、绕行、掩护、待命、撤退。

区域不应直接说“所有单位走到这里”；区域只提供持续生效的空间偏好。单位仍需结合兵种职责、距离、生命值、威胁、路径风险与当前战斗状态决定是否响应。

### 指令

玩家的指令应是低频的、可持续的，且可带条件。例如：

- 全军向集结区靠拢。
- 远程单位压制某片区域。
- 非火力单位避开火力覆盖区。
- 低血量单位撤往撤退区。
- 敌人进入某区域后再推进。

理想的操作流程：

```text
玩家：选择总战术，并部署一两个关键区域/例外
系统：将区域语义转为对 AI 的偏好
单位：按兵种职责和自身状态自主执行
玩家：只在危险、失控或战机出现时纠偏
```

为避免手忙脚乱，区域应限制数量、以锚点加固定半径快速创建、以战前配置为主；战中只允许快速新增、移除、移动或切换状态。复杂的手绘边界、逐组高频配置和逐单位控制都不是当前目标。

## 当前 Demo 的实现

运行入口为 [`index.html`](index.html)，区域模型在 [`tactics.js`](tactics.js)。不需要构建工具或第三方依赖。

当前交互：

- 选择区域类型，点击战场部署；最多 3 个区域。
- 右键已部署区域可移除；可一键清除全部区域。
- 敌军暂时不绘制区域，沿用原有自主 AI；这样更容易观察玩家指令是否真实改变战局。

当前代码边界：

```text
tactics.js      区域定义、数量限制、添加/移除、命中查询、绘制
index.html      单位模拟、AI、渲染、界面，以及对区域结果的解释
```

这是刻意的“轻工程化”第一步：先把区域从单文件战斗逻辑中抽离。待区域玩法稳定后，再依次将单位世界、决策、行为、渲染拆分为独立模块；此阶段不需要 ECS 或复杂框架。

## 敌方原则

敌方未来也应使用相同底层机制：区域、行为、条件和任务。差异只在于敌方区域由 AI 根据地图、目标与兵力自动生成，而不是由玩家绘制。

例如 AI 可自动建立核心防卫区、集结点、主攻方向和撤退/重整区，并在前排损失过大时切换任务。这样敌人呈现“守、攻、撤、绕”的意图，玩家也能读懂战局。

## 未来接入战斗组：如何解释同一片区域

战斗组不应让所有成员对区域做相同行为。它的价值是提供**任务上下文**；单位职责决定具体执行方式。

```text
区域语义 + 战斗组主任务 + 单位角色 + 单位状态
→ 局部意图
→ 具体行为
```

举例：火力覆盖区不是“所有人开火”或“所有人远离”。

| 角色 | 对火力覆盖区的局部意图 |
| --- | --- |
| 坦克 / 榴弹兵 | 寻找安全的可射击位置，优先打击区内目标。 |
| 机枪组 | 封锁区域边缘，压制进入或离开的敌人。 |
| 突击步兵 | 默认避开中心；承担突入任务时，等待火力暂停或从侧翼接近。 |
| 医疗兵 | 停在覆盖区外，照护撤出的伤员。 |
| 防空班 | 只有区域关联空中威胁时才改变行为，否则维持护卫职责。 |

同理，主攻组、护卫组与炮火支援组对同一集结区也应得到不同的局部意图：主攻成员进区整队，护卫成员守外围，火力支援在后方寻找可覆盖前沿的位置。

因此未来的数据关系应为：

```text
战斗组 = 成员 + 当前主任务 + 行为准则
区域   = 地点 + 语义 + 阵营 + 优先级 + 状态
单位   = 角色 + 状态 + 能力
```

区域解释器把它们组合为“候选意图”；AI 再按优先级、可行性和风险选择行为。不要把当前原型中按兵种写死的区域响应当作最终架构。

## 下一轮验证问题

1. 玩家是否能只放置 1–3 个区域，就稳定、直观地改变交战节奏与胜负？
2. 每类区域的效果是否足够可读，玩家是否能从单位站位、目标和攻击轨迹中理解原因？
3. 火力覆盖与危险区是否会造成不自然的往返移动，需要加入路径/进入惩罚而非“已在区内才逃离”？
4. 哪些区域在无战斗组前已有明确价值，哪些必须等任务上下文出现后才值得做？
5. 当答案稳定后，再决定战斗组的最小形态：成员、主任务、默认行为准则，而不是手动编队 UI。
