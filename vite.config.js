import { defineConfig } from 'vite';

export default defineConfig({
  // 旧的“自动战斗 + 战役地图”原型已整体移除（见 README）。
  // 后勤游戏原型的入口按《后勤游戏设计/技术实现方案.md》第二节实现后再加回来：
  // rollupOptions: { input: { logistics: 'logistics-demo.html' } }
});
